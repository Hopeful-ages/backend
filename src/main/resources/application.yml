# Configurações padrão (para produção/desenvolvimento)
spring:
  application:
    name: hopeful
  datasource:
    url: jdbc:postgresql://interchange.proxy.rlwy.net:20024/railway
    username: postgres
    password: LXraYmbJZgezowZqTLFGQrOkoaocICkt
    driver-class-name: org.postgresql.Driver

  flyway:
 
    baseline-on-migrate: true
    locations: classpath:db/migration 
    enabled: true
  jpa:
    hibernate:
      ddl-auto: none 
springdoc:
  swagger-ui:
    path: /swagger-ui
    enabled: true
    url: /v3/api-docs
    display-request-duration: true
  packages-to-scan: ages.hopeful
logging:
  level:
    root: INFO
    org.springframework.web: DEBUG
    org.hibernate.SQL: DEBUG
    org.hibernate.type.descriptor.sql.BasicBinder: TRACE

--- # Separador de documentos de perfil

spring:
  config:
    activate:
      on-profile: test
  datasource:
    url: jdbc:h2:mem:testdb;DB_CLOSE_DELAY=-1
    driver-class-name: org.h2.Driver
    username: sa
    password:
  jpa:
    show-sql: true
  flyway:
    enabled: true # Garante que o Flyway também execute para o banco em memória H2
